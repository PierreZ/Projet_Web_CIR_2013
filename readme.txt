Pour installer, il faut changer les données situées dans le dossier Setup. Il faut modifier le fichier php et le fichier JS !!!


Pour la base de données, les infos de connexions sont dans Entity/dao/bdd.php

Dans la base de données, il faut que période journéee contienne matin, apres-midi, journee (SANS ACCENT)
Dans la base de données il faut que type-de-compte contienne admin et utilisateur